To compile the TCP WIN multiclient server from ASSIGNMENT3-CONTENT:
```g++ '.\server\TCP WIN server multiClient.cpp' -lws2_32 -o server.exe```  
```.\server.exe```

To compile the TCP WIN multiclient client from ASSIGNMENT3-CONTENT:
```g++ '.\client\TCP WIN client multiClient.cpp' -lws2_32 -o client.exe```  
```.\client.exe```